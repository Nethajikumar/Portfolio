// This script is optional for additional animations or effects
document.addEventListener("DOMContentLoaded", () => {
    // You can add further JavaScript animations here if you want
});

function toggleMenu() {
    const navLinks = document.querySelector('.nav-links');
    navLinks.classList.toggle('active');
  }
